[package]
name = "nanoq_runtime"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
blake3 = "1.5"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokenizers = "0.20"

[profile.release]
opt-level = 3
lto = true
